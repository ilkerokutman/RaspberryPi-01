
enum BoilerMode {
  summer,
  winter,
  eco,
  weekly,
  custom,
}

enum FlameStatus {
  off,
  on,
}

enum ValueSuffix {
  none,
  celcius,
  bar,
}

enum ActiveTab {
  setValue,
  boiler,
  hotWater,
}
