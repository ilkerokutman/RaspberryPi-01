enum BoilerStatus {
  off,
  on,
}

enum BoilerMode {
  off,
  summer,
  winter,
}

enum FlameStatus {
  off,
  on,
}

enum WeatherStatus {
  clearSky,
  fewClouds,
  scatteredClouds,
  brokenClouds,
  showerRain,
  rain,
  thunderStorm,
  snow,
  mist,
}

enum ValueSuffix {
  none,
  celcius,
  bar,
}

enum DegreeControlTabItem {
  setValue,
  boiler,
  hotWater,
}
